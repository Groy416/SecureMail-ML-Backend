meta {
  name: List validations
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/validations?limit=1
  body: none
  auth: none
}

tests {
  test("returns paginated validations", function() {
    expect(res.status).to.equal(200);
    expect(res.body.records).to.be.an("array");
    expect(res.body.limit).to.equal(1);
  });
}
